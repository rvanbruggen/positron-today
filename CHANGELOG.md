# Changelog

All notable changes to Positron Today. Newest first.

| Version | Highlights |
|---------|-----------|
| **2.21.1** | Fix pipeline self-call being blocked by auth proxy on Vercel. The `after()` callbacks in `/api/pipeline/start` and `/api/pipeline/step` make server-to-server `fetch()` calls to `/api/pipeline/step` to chain the next processing step — but these calls carry no session cookie. The auth proxy in `proxy.ts` wasn't whitelisting `/api/pipeline/step` as a public path (unlike the older `/api/fetch-feeds` and `/api/classify` endpoints), so on Vercel the self-call received a 401 Unauthorized response. Because `fetch()` only rejects on network errors (not HTTP errors), the catch block never fired, the chain died silently, and the run stayed in `status='running'` with `phase='fetch'` and `sources_done=0` forever — producing the permanent "Phase 1 — scanning source 1 of N" display. Fix: add `/api/pipeline/step` to the proxy's public paths. Also harden both `after()` callbacks to check `res.ok` and throw on non-2xx responses, so future auth or routing issues surface as proper error states instead of silent zombies |
| **2.21.0** | Pipeline stop button and stale-run recovery. **(1) Stop endpoint + UI button.** New `POST /api/pipeline/stop` sets the active run's status to `'error'` with message "Cancelled by user", appends a `fatal` log entry so the UI renders it, and drains leftover `pending_items`. The existing step route already checks `status === 'running'` before proceeding, so the chain stops naturally on the next tick. The Preview page now shows a red **Stop** button next to "Fetching…" while a pipeline is running, and the progress label switches to "Stopped by user" on cancellation. **(2) Stale-run auto-recovery.** Two layers protect against permanently stuck pipelines (e.g. the `after()` chain silently dying on Vercel). The `/api/pipeline/start` endpoint now checks the age of any existing "running" row — if older than 10 minutes, it force-stops it as "Timed out (stale run)" and proceeds to create a fresh run instead of returning 409. The Preview page's on-mount check does the same: if it finds a running pipeline older than 10 minutes, it auto-stops it via the stop endpoint rather than re-attaching to a zombie. Together these ensure the UI never gets permanently stuck showing Phase 1 of a dead run |
| **2.20.1** | Fix the server-side pipeline's progress bar never updating after the initial "source 1 of N" state. Three root causes. **(1) Missing `maxDuration` in `vercel.json`.** The new `pipeline/start` and `pipeline/step` routes added in 2.20.0 were not listed in `vercel.json`, so they ran with Vercel's default function timeout (~10-15s) instead of the intended 60s — easily exceeded when processing 15 RSS feeds with per-item duplicate checks against Turso, causing the function to be killed before it could update counters or chain the next step. Added `maxDuration: 60` for both routes. **(2) Unquoted `offset` column in dynamic SQL.** The `updateRun()` helper in `pipeline/step` built SET clauses with bare column names (`offset = ?`); `OFFSET` is a SQL keyword and could cause the UPDATE to error on some SQLite/libsql versions, silently preventing `sources_done` and other counters from being persisted. All column names in `updateRun()` are now double-quoted (`"offset" = ?`). The `pipeline/status` SELECT query had the same issue and is also quoted. **(3) Stale polling responses.** The `pipeline/status` GET endpoint had no caching directive, so Next.js production builds and/or browser caches could serve stale progress data. Added `export const dynamic = "force-dynamic"` to the route and a `&t=` cache-buster to the polling fetch in the Preview page UI |
| **2.20.0** | Move the fetch-and-classify pipeline orchestration from client-side to server-side so closing the browser or backgrounding the mobile app no longer interrupts a run. Previously the Preview page's "Fetch new articles" button drove a multi-request loop from JavaScript — calling `/api/fetch-feeds` in chunks then `/api/classify` in batches, waiting for each response before firing the next. On desktop browsers that worked fine (the tab keeps running), but on mobile the OS suspends background tabs aggressively, killing the orchestration mid-flight. Now the button fires a single POST to a new **`/api/pipeline/start`** endpoint, which creates a `pipeline_runs` row in the DB and returns a `runId` immediately. The actual work happens server-side via **`/api/pipeline/step`**, a self-chaining endpoint that uses Next.js `after()` to fire the next step after each response completes — fetch chunk 1 → fetch chunk 2 → … → classify batch 1 → classify batch 2 → … → export rejection log → mark done. Each step fits comfortably within Vercel's 60s Hobby-tier `maxDuration`; `after()` ensures the next invocation is triggered even after the HTTP response is sent and the client has disconnected. The UI polls **`/api/pipeline/status?runId=X`** every 2 seconds for progress, log lines, and phase information; the progress bar and NDJSON-style log panel render identically to before. If the user navigates away and returns to the Preview page, an on-mount check detects any running pipeline and resumes polling automatically — no work is lost. New `pipeline_runs` table (idempotent migration in `admin/lib/schema.ts`) tracks status, phase, cumulative counters (queued/classified/added/filtered/errored), queue depth, and a JSON log column. New `admin/lib/self-url.ts` helper resolves the app's own base URL for server-to-server calls (`VERCEL_URL` on Vercel, `localhost` in dev). Overlap protection: `/api/pipeline/start` returns 409 if a run is already in progress, and the UI attaches to the existing run instead of starting a duplicate. All existing endpoints (`/api/fetch-feeds`, `/api/classify`, `/api/fetch`) are untouched — the Synology cron and any other external callers continue to work exactly as before |
| **2.19.0** | Split the RSS fetch pipeline into two phases so a growing source list (now 42, with more arriving in non-EN/NL/FR languages) stops bumping into Vercel's 60s `maxDuration` ceiling. The legacy `/api/fetch` route did everything in one invocation — pull RSS → dedup → LLM positivity filter — and the LLM call dominates per-item cost, so a single chunk could blow the budget if a few sources happened to have many genuinely new items at once (each classify ≈ 1–3s, easily 50 calls per chunk in the worst case). Now there are three logical steps as two HTTP endpoints. **(1) `/api/fetch-feeds` — Phase 1, no LLM.** Pulls RSS for a chunk of sources (default `?limit=15`, larger than the legacy `5` because no LLM means each source costs ~1–2s worst-case bounded by the existing 8s `RSSParser` timeout), dedups every item against `raw_articles` / `rejected_articles` / `articles` AND a new `pending_items` queue table, then inserts genuinely new items into `pending_items`. Streams the same NDJSON shape as `/api/fetch` so the UI can show progress in real time. Auto-mode rotates through chunks via the existing `auto_fetch_offset` setting, identical to before. **(2) `/api/classify` — Phase 2, LLM-bounded.** Pulls a batch from `pending_items` (default `?limit=15`, JOINed against `sources` for the language column so per-item DB chatter is one query not two), runs each through the positivity filter, and routes the result into `raw_articles` (accepted) or `rejected_articles` (rejected) — same logic, prompt, and translation handling as the legacy route — then deletes the queue row. Errors leave the row in place so a future run retries naturally. The rejection-log export to the public site (`exportRejections()`) moved here, fired only when the queue fully drains, so it runs once per cycle instead of per chunk. New `pending_items` table is a 6-column staging area with `UNIQUE(url)` and an `ON DELETE CASCADE` to `sources`; idempotent migration in `admin/lib/schema.ts`. **(3) Preview page button drives both phases sequentially.** The "Fetch new articles" button on `/preview` now loops `/api/fetch-feeds` chunks until all sources are exhausted, then loops `/api/classify` batches until the queue is empty. The progress bar splits 50/50 between the two phases and the NDJSON log stream interleaves Phase 1 and Phase 2 events with a "▸ Phase N" sectioning header. New summary bar at the bottom aggregates `added` / `filtered` / `queued` / `skipped` / `errored` across every chunk and batch — previously only showed the last `done` event, which lost information when chunking was active. **(4) Legacy `/api/fetch` kept operational** for the Synology cron's transition window — its dedup check now also looks at `pending_items` so the two pipelines can't fight over the same URL. Recommended migration: replace any `/api/fetch?auto=1` cron with two entries (`/api/fetch-feeds?auto=1` and `/api/classify?auto=1`); they can run on the same schedule or different ones, and items survive in `pending_items` between calls so neither needs to "complete" inside any single 60s window. New routes whitelisted in `proxy.ts` and added to `vercel.json` with `maxDuration: 60` |
| **2.18.0** | Public About page now renders non-EN/NL/FR sources in their own group instead of silently filing them under English. The 17-language input feature added in 2.17.0 meant any source tagged German, Spanish, Danish, Swedish, Polish, "auto", etc. needed somewhere to go on the public site, but `admin/lib/export-sources.ts` had a hardcoded fallback bucket of "en" for any unknown language code — so Politiken.dk (DA), Frankfurter Allgemeine (DE), Neue Zürcher Zeitung (DE), El País (ES), Rzeczpospolita (PL), Dagens Nyheter (SV), and anything else outside the three site-output languages all showed up under "🇬🇧 English" on `/about/`, mixed in with the actual English sources. Two-part fix. **(1) Export keeps every language distinct.** The three native buckets (en/nl/fr) are unchanged; everything else collects into a new "other" bucket, sorted by language code first then by name. Each pill in the "other" bucket also carries its language code in the JSON so the template can show it alongside the source name. **(2) About template renders a fourth source group**, "🌍 Other languages / 🌍 Andere talen / 🌍 Autres langues", iterating over `sources.by_language.other` and appending a small uppercase language-code badge after each pill name (e.g. *El País ES*, *Frankfurter Allgemeine DE*, *Politiken DA*). The orange feed-dot semantics carry over unchanged. After upgrading, click "Publish to site" on the admin Sources page once so the existing `site/src/_data/sources.json` is re-exported with the new bucketing — the misclassified rows will move from English to Other on the next render |
| **2.17.1** | Three small fixes around the public About page's source list. **(1) Source pills no longer 404.** Several rows in the `sources` table had legacy URLs without a scheme (`www.theguardian.com`, `www.theverge.com`, `www.wired.com`, `www.nrc.nl`, `www.gva.be`) which the browser was resolving as RELATIVE paths against `/about/` → `https://positron.today/about/www.theguardian.com` → 404. New `normaliseSourceUrl()` helper in `admin/lib/export-sources.ts` trims whitespace, lower-cases the scheme if present, and prepends `https://` if missing — applied to every URL on the way out, so the JSON committed to `site/src/_data/sources.json` is always safe to drop into an `<a href="...">`. The 5 affected DB rows were also normalised in place via a new `admin/scripts/normalise-source-urls.ts` script (dry-run by default, `--apply` to write — same pattern as the existing `cleanup-website-only-sources.ts`) so the admin Edit form now shows clean values too. **(2) About-page copy: "RSS feeds" → "online sources" in all three languages.** The pipeline diagram caption ("RSS feeds scanned automatically, multiple times a day") and the introductory paragraph above the source list ("Sources with an orange dot are fetched via RSS; others are added manually") used technical RSS terminology that wasn't a great fit for the public site's voice. Both spots now use vaguer "online sources" / "online bronnen" / "sources en ligne" wording, with the orange-dot legend reworded to "picked up regularly" vs "added manually, one article at a time" so it stays informative without naming the underlying transport. The "Subscribe via RSS" section further down on About, the `<link rel="alternate" type="application/rss+xml">` tags in `base.njk`, and the footer "RSS ↗" links are deliberately untouched — those describe the site's *own* publication channel and stay clearly labelled. **(3) Sources page language dropdown sorted alphabetically.** With 17 entries (auto-detect + 16 European languages) added in 2.17.0, the previous "three native first, then everything else in some order" arrangement was hard to scan. Auto-detect stays pinned at the top; the rest of the list now sorts alphabetically by English label (Czech → Danish → Dutch → English → Finnish → French → German → Greek → Hungarian → Italian → Norwegian → Polish → Portuguese → Romanian → Spanish → Swedish), driven by a single ordering in `admin/lib/languages.ts` that both selects on the Sources page (add form + inline edit) read from |
| **2.17.0** | Multilingual input sources with auto-translated English previews and cross-language deduplication. Sources can now be tagged with any of 17 input languages — `auto` (auto-detect), the three existing output languages (`en` / `nl` / `fr`), and 13 common European languages (`de`, `es`, `it`, `pt`, `da`, `sv`, `no`, `fi`, `pl`, `cs`, `ro`, `el`, `hu`) — instead of being limited to en/nl/fr. The summarise step was already language-agnostic (the LLM produces all three output languages from any input), so the only thing that changed for non-native sources is the review surface: at fetch time the positivity-filter prompt now also asks the LLM for a short English title + 1-2-sentence English snippet whenever the source language isn't en/nl/fr (single LLM call, no extra round-trip), stored in two new `raw_articles` columns `preview_title_en` / `preview_snippet_en` (idempotent migration in `admin/lib/schema.ts`). The Preview page renders these under the original-language title in a small blue "English preview" block, with a per-card source-language badge so the reviewer sees at a glance what they're looking at. Duplicate detection on the Preview queue (`admin/app/api/articles/route.ts`) extended into English space — non-native pending raws enter the dedup pool tagged `language="en"` using their English-preview tokens, and recent NL/FR articles get a second pool entry built from their `title_en` so cross-language matches fire bidirectionally (e.g. a Spanish article and an English article about the same event will now warn each other). Native NL/FR pending raws still dedup in their own language only — they don't have an English title at the raw_articles stage. New shared `admin/lib/languages.ts` module owns the dropdown list + the `isNativeOutputLanguage()` helper; both selects on the Sources page (add form + inline edit) now render from this single source instead of hardcoding three options each |
| **2.16.5** | Stop the `/api/fetch` route from getting stuck against Vercel's 60s `maxDuration` cap on the Hobby plan. Vercel logs were showing recurring `FUNCTION_INVOCATION_TIMEOUT` (504) on `app/api/fetch/route.ts`, with the streamed NDJSON response cut off mid-chunk and the auto-fetch offset advancing past sources that hadn't actually been processed. Three reliability fixes in `admin/app/api/fetch/route.ts`. **(1) Per-feed timeout on `RSSParser`.** `rss-parser` ships with no default timeout, so a single hanging RSS host (slow DNS, dead origin holding the TCP connection open, etc.) could eat the entire 60s budget while the other 9 sources in the chunk never got a chance to run. The module-level parser is now constructed with `new RSSParser({ timeout: 8000 })` — a stuck feed costs at most 8s and fails fast into the existing `source_error` branch, leaving the rest of the chunk to proceed. **(2) Default chunk size 10 → 5.** The `?limit` query-param default dropped from 10 to 5 so each cron tick fetches half as many sources per invocation. The auto-mode offset rotation is unchanged (it still wraps to 0 on the last chunk and persists `auto_fetch_offset` on every call), so the only behavioural effect is that a full pass over all sources now takes twice as many cron ticks — at the gain of comfortably fitting under the 60s ceiling. Manual runs from the admin UI can still override with an explicit `?limit=`. **(3) Items per feed 20 → 10.** `feed.items.slice(0, 20)` is now `slice(0, 10)`. The LLM `classify()` call dominates per-item cost, so halving the cap directly halves worst-case time-per-feed; in practice most healthy feeds only have 1–3 genuinely new items per cycle (the rest are skipped via the existing `raw_articles` / `rejected_articles` / `articles` URL-uniqueness pre-checks), so the cap reduction has minimal impact on coverage. Net effect: worst-case per-invocation time is now ≈ 5 × max(8s feed timeout, classify cost for ≤10 items) — well clear of 60s on a Hobby plan |
| **2.16.4** | Two small admin/site-visible fixes. **(1) Positronitron run-times cap lifted.** The "+ Add time" button on the Positronitron card of the admin Settings page (`admin/app/settings/page.tsx`) was gated by `{runTimes.length < 4 && …}` and the helper text read *"1–4 daily run times"* — both removed. The save path, the `findDueSlot` logic in `/api/positronitron`, the JSON-array store, and the `positronitron_run_times` DB column had no length enforcement of their own (the cap was UI-only), so users can now schedule any number of daily auto-runs. The remove (✕) button still requires `runTimes.length > 1`, preserving the "off-mode is the only way to fully disable Positronitron" invariant. **(2) Card permalink icon now visible on the Classic theme.** The tiny Positron-atom icon in the bottom-right of every homepage / archive-month / tag-landing card — the one that links to the Positron Today summary page when the card itself goes straight to the source — was rendering at effective opacity ≈ 0.24 on the default Classic theme because `.card-meta { opacity: 0.6 }` (parent) and `.card-permalink { opacity: 0.4 }` (child) compose multiplicatively. Mono and Mondrian dodged this via existing `.card-meta { opacity: 1 }` overrides; tag landing pages were broken on every theme since they carried no overrides at all. Fix: move the muting opacity from the parent `.card-meta` / `.tag-card-meta` to its non-permalink span children only (`site/src/index.njk`, `site/src/archive/month.njk`, `site/src/tags/tag.njk`), so the permalink keeps its intended 0.4 visibility on every theme without parent attenuation. Added compensating `opacity: 1` overrides for Mono/Mondrian's text spans in `site/src/assets/themes.css` so those themes (which mute via `--mono-muted` / `--md-muted` color, not opacity) don't end up double-muted on the source/date text. Source/date appearance unchanged on every theme; only the permalink icon's effective opacity changed |
| **2.16.3** | Show date *and* time on every surface that displays a rejection timestamp, in a single consistent timezone. Previously the public negativity page, the public-site export, and the admin Rejections page all called `String(fetched_at).slice(0, 10)` on the SQLite UTC timestamp — keeping only the date portion and silently throwing away the time. The DB column has always stored full second-precision timestamps, just nothing rendered them. New `formatRejectionTimestamp(sqliteUtc)` helper in `admin/lib/schedule-time.ts` parses the SQLite UTC string ("YYYY-MM-DD HH:MM:SS"), normalises it to ISO + Z, and returns "YYYY-MM-DD HH:MM" wall-clock in `SCHEDULE_TZ` (defaults to Europe/Brussels) — so admin and public site share one clock with the rest of the scheduling stack. Wired through `admin/lib/export-rejections.ts` (the JSON committed to `site/src/_data/rejections.json` now carries date+time in the `date` field) and `admin/app/rejections/page.tsx` (replaces both `slice(0, 10)` call sites; the title attribute clarifies "Brussels local time" on hover). The `source_pub_date` column in the admin Rejections page is intentionally left date-only — that's the publisher's pub-date from the RSS feed, a different datum. New "Times and dates" section on the public About page (`site/src/about.njk`) explains the choice in EN/NL/FR: all timestamps are Brussels wall-clock (CET/CEST, i.e. UTC+1 in winter, UTC+2 in summer); readers from other timezones should adjust. The helper degrades gracefully — returns the raw input on parse failure rather than rendering "NaN-NaN-NaN" |
| **2.16.2** | Defensive fix in `OpenAIProvider.call()` (`admin/lib/llm.ts`) so the positivity filter and summariser don't break if the user selects an OpenAI reasoning model (`o1*`, `o3*`, …) for either task on the Settings page. Reasoning models reject the `temperature` parameter outright and require `max_completion_tokens` in place of `max_tokens`; the previous code sent both unconditionally, so any o-series selection would 400 on every fetch — silently breaking the entire pipeline until the user noticed and switched back. Now branches on a regex against the model name (`/^o[1-9]/i`): GPT-family models (`gpt-4o`, `gpt-4o-mini`, `gpt-4-turbo`, …) keep their existing `max_tokens` + `temperature` body; o-series get `max_completion_tokens` and no `temperature`. The regex deliberately matches `o<digit>` so future o-series releases (`o4`, `o5`, …) work without code changes, while `gpt-4o*` correctly does not match (it starts with `gpt-`, not `o`). Anthropic and Ollama provider paths unchanged. Pure correctness fix; no new settings, no migrations |
| **2.16.1** | Make the positivity-filter `classify()` path deterministic across all three providers. The Anthropic implementation in `admin/lib/llm.ts` previously sent no `temperature` parameter at all, so the API default (~1.0) applied — meaning the same article + prompt could classify YES one minute and NO the next, and rules buried mid-paragraph in the filter prompt (sports rejection, "global relevance" preference) were getting skipped on borderline cases instead of consistently enforced. The Ollama and OpenAI implementations both passed `0.3`, also more than ideal for a binary classification call. All three `classify()` paths now pass `temperature: 0` for a stable yes/no decision. The `generate()` path used for summarisation is unchanged on every provider — it keeps `0.3` because creative summary writing benefits from non-zero temperature. Implementation: parameterised the existing private `callOllama` / `call` helpers with a `temperature` argument rather than hardcoding it inside the request body, so the two callers can pass different values without duplicating the fetch payload. Anthropic's `messages.create` call gains a one-line `temperature: 0`. No new settings, no migrations — purely a default-behaviour fix |
| **2.16.0** | Three workflow fixes on the admin Preview → Scheduled pipeline. (1) Overriding a human-rejected article from the Rejections page now actually puts it back into the review queue. Previously the POST handler in `admin/app/api/rejected/route.ts` always tried to INSERT the article URL back into `raw_articles`, but when the rejection originated from a human discard on Preview the `raw_articles` row was still there with `status='discarded'` — the unique-URL pre-check always tripped a 409 *"Already in review queue"*. Now the handler inspects the existing row: if its status is `'discarded'` it flips back to `'pending'` (and the corresponding `rejected_articles` entry is still deleted, keeping both tables in sync); only genuinely active rows (`pending` / `approved`) still return 409, with the actual status surfaced in the error message. (2) Freshly-summarised articles now land on the Scheduled page's "Ready to publish" list with a suggested publish slot already filled in, instead of showing up with an empty datetime input that the user had to set manually or bulk-assign via the existing "📅 Suggest schedule" button. `/api/summarise` computes a `nextSlot` using the same `lib/schedule-time` helpers as `/api/suggest-schedule` — 30-minute interval, jittered, respecting `SCHEDULE_TZ` — chained after the latest currently-scheduled `publish_date` (or from "now" if nothing is scheduled) and writes it as part of the same UPDATE that sets `status='scheduled'`. A pre-existing `publish_date` (e.g. set manually, or on re-summarise) is preserved. The response now carries `publish_date` + `image_url` so `ScheduledClient` state updates in place with no reload. (3) The `og:image` captured during summarisation is now rendered as an 80×80 rounded thumbnail next to the emoji in each "Ready to publish" card, so the reviewer can tell at a glance whether an article has a decent hero image — useful signal when deciding whether to tick the "⭐ Featured" or "📣 Announce on social" checkboxes. Threaded `image_url` through `app/scheduled/page.tsx` → `ScheduledWrapper` → `ScheduledClient` types; `<img onError>` hides the element if the og:image URL 404s, same graceful degradation as History |
| **2.15.3** | Fix stale copy on the admin Settings page Positronitron section. The helper line below the run-times picker previously read *"Schedule: [times] daily via launchd. The hourly publish job handles the actual publishing."* — both the launchd reference and the hourly cadence were implementation details true only for local dev (launchd plist agent hitting localhost hourly). In production the Positronitron auto-runs are triggered externally and the scheduled-publish job runs every 30 minutes via a Synology NAS cron, not hourly via launchd. Reworded to *"Schedule: [times] daily. A separate publish job handles the actual publishing of any articles Positronitron schedules."* — no implementation details, accurate for both local and production. One-line change in `admin/app/settings/page.tsx`; the `/api/positronitron-schedule` endpoint and its launchd-plist generation are unchanged (still useful for anyone running admin locally) |
| **2.15.2** | Add a Facebook profile link to the Contact page and regenerate the social profile banners for all three platforms. `site/src/contact.njk` gains a fourth social row below X in each EN / NL / FR block (📘 icon, "Positron Today" label, localized note `Also on Facebook` / `Ook op Facebook` / `Aussi sur Facebook`, hrefs to `https://www.facebook.com/people/Positron-Today/61573473671357/`). For the profile banners, replace the single-size `admin/scripts/generate-social-banner.mjs` (which produced one 1500×500 PNG for both X and Bluesky) with a parameterised `generate-social-banners.mjs` (plural) that renders the same satori + sharp pipeline at three aspect ratios in one pass — `social-banner-facebook-820x312.png`, `social-banner-twitter-1500x500.png`, `social-banner-bluesky-3000x1000.png`. Design tweak motivated by Facebook mobile cropping the sides of cover photos: the `positron.today` URL moves out of the top-right corner (where Facebook was clipping it off) into the centred content stack, below the tagline. Every dimension in the layout now scales relative to banner height, so the three exports share identical proportions at their native resolutions. Old `branding/social-banner-1500x500.png` + old singular generator script removed; regenerate any time with `cd admin && node scripts/generate-social-banners.mjs` |
| **2.15.1** | Fix the wrong Bluesky handle on the Contact page. The EN / NL / FR sections of `site/src/contact.njk` all linked to `https://bsky.app/profile/positrontoday.bsky.social` and displayed `@positrontoday.bsky.social`, but the actual profile lives at the custom domain handle — `@positron.today` / `https://bsky.app/profile/positron.today`. Updated all six occurrences (three `href`s + three visible `contact-link-value` labels). The Bluesky share-intent URL in `post.njk` is unaffected (it's a compose-intent, not a profile link). No other files reference the old handle |
| **2.15.0** | Mobile / PWA homepage redesign so phone visitors see several articles above the fold instead of one full card filling the viewport. Under `@media (max-width: 699px)`, the top-featured section in `site/src/index.njk` flips `.home-top-featured` to `flex-direction: row` so the two featured cards render as side-by-side hero tiles (image on top via the existing `.card-img` bleed rule, title below, everything else — summary, emoji, tags, meta row — hidden). Every other card in the main feed picks up a new `card-mobile-compact` class that collapses it into a flex-row: 80×80 square thumbnail (`aspect-ratio: 1/1`, `object-fit: cover`) on the left, 3-line-clamped title on the right. The main feed is also forced back to 1 column on phones (`calc(100% - 1rem)` overrides the 480px+ 2-col Masonry rule) so compact rows are full-width and tap-friendly. Result on iPhone 375×812: 5 articles visible above the fold (2 hero tiles + 3 compact rows), up from 1 full card previously. Mobile also swaps the active click target on every card: two `<a class="card-link">` elements now render per card — `.card-link-desktop` (`target=_blank` → `source_url`) and `.card-link-mobile` (→ `post.url`, the Positron intermediate page) — toggled by `display:none` inside the same mobile media query. Rationale: tapping an external link from a standalone PWA drops the user out of the app shell, and on mobile the inline summary is hidden anyway, so the intermediate page is the natural next step; the Positron page still has a prominent "go to source" button so it's one extra tap, not a dead end. Desktop (≥1100px) and tablet (700–1099px) are completely untouched — full cards with summaries in their respective layouts, click still opens the source in a new tab |
| **2.14.3** | Adopt the MIT License and surface it in the footer of every page. New `LICENSE` at the repo root — standard MIT text with Rik Van Bruggen (2026) as copyright holder, plus an explicit note that the licence covers source code only and that article/summary/image handling is described separately in `LEGAL-NOTES.md`. Both footers now link to the `LICENSE` file on GitHub: the admin chrome in `admin/app/layout.tsx` reads *"Positron Today admin · v{version} · MIT License"*, and the public-site chrome in `site/src/_includes/base.njk` ends with *"· v{version} · MIT License ↗"* (unconditional anchor, so it shows identically on EN / NL / FR). `"license": "MIT"` added to every `package.json` (admin, site, root — site previously had an npm-init default of `"ISC"`). Also clean up the documentation: the 70+ row Version History table moved out of `README.md` into its own `CHANGELOG.md` (this file), cutting README from 524 → 450 lines; the Version Tracking section in README now lists the four canonical files to update on every bump (`admin/lib/version.ts`, `admin/package.json`, `README.md` badge, `CHANGELOG.md` new row) and drops the stale reference to the root `package.json` as a sync target (it hasn't been touched since 2.8.1) |
| **2.14.2** | Require an RSS feed URL on every source added via the Sources admin page, now that one-off URL submission lives on the Preview tab (→ `/api/manual-url` → auto-created "Manual" source). The `/api/sources` POST + PATCH handlers reject missing `feed_url` and always insert `type='rss'`; OPML import skips entries without an `xmlUrl` (logged as `"no RSS feed URL"` in `skippedInvalid`); the Sources page drops the `type` field from the add form, marks `feed_url` required on add + edit, and no longer renders the misleading "Website only — add articles manually" section. The auto-created "Manual" source is untouched — `/api/manual-url` inserts it via a direct DB call that bypasses the new validator. Also ship `admin/scripts/cleanup-website-only-sources.ts` (dry-run by default, `--apply` to delete) for removing legacy website-only rows; published articles survive, pending `raw_articles` are wiped per source |
| **2.14.1** | Hotfix on top of 2.14.0 — Vercel build was failing with `Property 'bold' does not exist on type '{ href: string; label: string; }'` because the MobileNav destructured `bold` and `icon` off each `NAV_LINKS` entry, but those properties only existed on the (now removed) Fast Track entry. Drop the dead destructure variables and the `${bold ? ... : ...}` className branch; the desktop nav already lacked them so this just brings MobileNav into line. Pure type-error fix, no UI change |
| **2.14.0** | Surface the prompts auto runs will use directly inside the Positronitron card on `/settings`. Two collapsible details now show, by current state: *"Positivity filter: threshold N — &lt;tier&gt;"* (or *"custom override"*) and *"Summarisation style: default style"* (or *"custom override"*) — expand either to see the full prompt text the LLM will receive on the next auto run. Closes a real source of confusion: the Positronitron section previously gave no hint that auto runs read the same `filter_prompt_override` / `summarise_style_override` values as manual runs do, so it wasn't obvious which strictness tier auto was actually applying. Also retire the entire Fast Track surface — the `/fast-track` page + `/api/fast-track` route + nav links + dashboard quick action + vercel function entry — now that the Positronitron automation modes cover the same ground without the hardcoded threshold-10 override that bypassed user prompt customisation |
| **2.13.3** | Fix 404 social-post previews when publishing manually from the Scheduled page. The "Publish →" / "publish now ↑" buttons in `admin/app/api/publish/route.ts` were calling `postArticleToSocial` inline, right after the GitHub commit — long before Pages had actually deployed the new post, so Bluesky / X / etc. fetched a 404 for the link preview. Removed the inline social call so articles with `post_to_social_on_publish=1` now sit in the same pending state the cron path uses (`status='published'`, `social_posted_at IS NULL`); the Pages-deploy workflow's callback to `/api/post-pending-social` picks them up once the URL is live, same as scheduled publishing |
| **2.13.2** | Strip the featured signal entirely from archive month pages. Archive is a purely chronological historical browse — featured is an editorial "interesting now" signal that doesn't apply to past months — so featured and non-featured posts now look identical on `/archive/YYYY/MM/`. Removes the `card-featured-display` class from the archive template and drops its CSS rules, so the 4-column archive grid reads as a uniform historical list with no visual echo of the homepage's featured treatment |
| **2.13.1** | Two polish fixes on top of 2.13.0. Trim the desktop featured rail so it ends roughly where the main feed ends — previously, when there were far more featured articles than regulars the right column ran much longer than the left, leaving a big whitespace hole at the bottom of the left column. Now a small post-Masonry JS pass hides any rail card whose bottom would extend past the feed's bottom (with an 8px tolerance), and the pass re-runs on filter changes and on window resize so the alignment holds. Also extend the same layout cleanup to archive month pages (`/archive/YYYY/MM/`): the old `.card-featured` double-width class is gone there too, replaced with the same `.card-featured-display` visual treatment (larger headline, image-first, no emoji) used on the homepage, so featured posts still stand out but the 4-column archive grid stays uniform |
| **2.13.0** | Overhaul how the homepage surfaces featured articles so the "wider column" messiness goes away. Out: the old `.card-featured` double-width class that forced Masonry to shuffle 2-column-wide cards into a 1- to 4-column grid and left visible white-space gaps when a wider card couldn't fit the remaining row. In: a region-based architecture using CSS Grid. Desktop (≥1100px) runs a 60/40 split — a 2-column Masonry feed on the left, a dedicated featured rail on the right with top-2 featured above and featured #3+ stacked below. Tablet (700–1099px) shifts the top 2 featured into a 2-column strip above the 2-col feed; the rail is hidden. Mobile (<700px) stacks the top 2 featured and flows the rest of the feed single-column below. Featured cards themselves get a visual treatment (larger headline, image-only, no emoji) by virtue of living in a featured region — no more width-based differentiation, so Masonry never has to place mixed-width cards and no more gaps. Featured #3+ is duplicated into the main feed DOM with `.feed-dup-featured` (hidden on desktop via CSS) so tablet/mobile still surface them, styled as regular cards. Filters (tag + date) now apply to all three regions in lockstep. Empty-rail fallback borrows the most-recent non-featured posts when fewer than 2 featured exist, as a safety net |
| **2.12.0** | Per-topic landing pages at `/tags/<slug>/` — every tag used across posts now has its own crawlable page listing every story filed under it, plus an index at `/tags/` with count-sorted pills in the same colour palette as the card grid. Homepage, archive month views, and individual post pages now turn tag pills into links (`<a>` at `z-index:2` so they intercept clicks above the card-source overlay), creating internal links for SEO and letting readers browse by topic without filtering. Sitemap extended with the new tag URLs. Also fix two SEO defects surfaced by an audit: the per-post `meta description` / `og:description` was serving the generic site blurb on all 285 posts (a `{% set description %}` inside `post.njk` wasn't reaching its parent `base.njk` — replaced with an `eleventyComputed` directory data file at `posts/posts.11tydata.js` so each post now serves its own summary), and homepage / archive card images had `alt=""` (now carry the article title, matching what post hero images already did) |
| **2.11.0** | Rewrite public-site chrome voice in all three languages: drop every "we/us/our" from templates (nav, About, negativity page, Contact) for legal reasons — the site is run by one individual, not a legal entity — and shift automation descriptions to passive / system voice (`"What gets skipped"`, `"The following news sources get scanned"`) rather than first person, reserving "I" for personal-narrative copy like the About-page origin story. Add PWA auto-update for both the public site and the admin: the service-worker registration URL now includes `?v=<APP_VERSION>`, so each deploy yields a fresh SW script and cache name, the `activate` handler purges old caches, and an `updatefound → activated` listener reloads open tabs — installed PWAs pick up new content and functionality immediately instead of after the browser's ~24h refresh window. Fix the `"NO GLYPH"` watermark that Satori was painting on some Instagram cards: the card renderer only loaded Playfair Display + Inter, neither of which contains emoji glyphs, so any emoji (including the giant hero-fallback emoji when a source image failed to prefetch) rendered as the literal placeholder — now wired through Twemoji SVGs via satori's `loadAdditionalAsset` with a module-level cache |
| **2.10.2** | Tighten `LEGAL-NOTES.md` to state explicitly that article images come from the publisher's `og:image` meta tag only (no scraping of article-body `<img>`s, no fallback), which matches the code in `summarise/route.ts` and `positronitron/route.ts`. Add a tri-lingual footnote-style link from the About page's copyright subsection to the legal notes on GitHub so publishers / lawyers can find the technical description directly from the public site |
| **2.10.1** | Soften the copyright wording on the About page: drop the "fair use / fair dealing" phrasing, which is US/UK-legal-system-specific, in favour of a neutral statement — "I rely on standard conventions of commentary, curation and quotation, with attribution and a direct link to the source." Translations use the correct jurisdiction-specific legal terminology where it exists: `het citaatrecht` (Dutch Auteurswet) and `le droit de citation` (French Code de la propriété intellectuelle) |
| **2.10.0** | Redesign the click target on homepage + archive cards: the card now opens the original source article in a new tab (the primary user flow — browsing). A small atom-icon permalink in the card-meta row still leads to the Positron Today page for the story, which remains the target for social unfurls, RSS items, and search results — the plumbing stays intact. Added an extensive "A personal, not-for-profit initiative" section to the About page (tri-lingual) stating that the site is run by one individual with no commercial motive, explicitly respects source copyright, and exists to *promote* the linked journalism rather than replace it |
| **2.9.2** | Correct the `/install` page instructions (and the iOS fallback modal): drop the "Share button at the bottom" claim — the Share button is at the top on iPad, iPhone in landscape, and iPhone with Single Tab layout. Describe it visually ("square with an arrow pointing up") instead. Add "Scroll down" to the iOS steps so users don't miss the Add-to-Home-Screen entry. Change "Tap" → "Click" on desktop; drop the poorly-rendered 🖳 emoji in favour of a clearer "install icon" description. Note the Android "Install app" / "Add to Home screen" label variance |
| **2.9.1** | Fix flaky typing in the Scheduled page's "Ready to publish" datetime input. The input was controlled + fetch-on-every-keystroke, so partial/empty intermediate values (e.g. while clearing a segment) got PATCH'd to the server, the async round-trip snapped the value back mid-edit, and the `toDatetimeLocal` fallback teleported empty states to "today". Switched to uncontrolled (`defaultValue` + `key`), commit only on blur with regex validation, revert on incomplete — typing now flows naturally |
| **2.9.0** | Fix social posts that landed against not-yet-deployed URLs (the cause of 404 link previews on Bluesky / X / etc.). Decoupled commit from social: `/api/publish-scheduled` now only commits to GitHub and returns fast; new `/api/post-pending-social` endpoint is called by the GitHub Pages deploy workflow once the URL is actually live, then posts to social with a guaranteed-200 link. Bearer-token auth (`SOCIAL_POST_TOKEN` env var on Vercel + matching repo secret on GitHub) so only the deploy workflow can trigger it |
| **2.8.3** | Refine featured-article highlight on History: drop the ⭐ prefix; render the title in bold red (`text-red-700`, hover `text-red-500`) instead. Cleaner scan, no emoji clutter |
| **2.8.2** | Visually flag featured articles on the admin History page — titles render in bold with a ⭐ prefix so the reviewer can see at a glance which posts are featured on the public site (wide cards) versus regular |
| **2.8.1** | Wire the `/install` page into the main nav (after Contact) in EN / NL / FR; add theme overrides so the page renders correctly under the Mono and Mondrian colour schemes (Classic was already correct). Fix stale site footer version: the Eleventy meta data was pulling from the rarely-updated root `package.json` — now reads from `admin/package.json`, the real source of truth, so future bumps propagate automatically |
| **2.8.0** | Shareable PWA install flow on the public site: dedicated `/install` page with per-platform instructions, one-tap install button (native prompt on supported browsers, iOS modal fallback), and a branded QR code (atom mark centred in a rounded card) that points back to `/install/`. The `?install=1` query param on any page auto-opens the install flow. New `admin/lib` script generates the QR; a shared `install.js` exposes a `window.PositronInstall` API so other pages can add their own install CTAs later |
| **2.7.1** | PWA icon swapped from the speech-bubble logo to the bright Positron atom mark (same geometry as the admin nav bar, rendered on a cream background with maskable-safe padding) |
| **2.7.0** | Public site is now an installable PWA. Added `/manifest.webmanifest` (English-default, bright speech-bubble icon at 192/512 on a cream background, maskable-safe padding), `/sw.js` (network-first with cache fallback so articles stay readable offline), Apple / Android install meta tags, and an SW registration snippet in the base layout. Visitors get an "Add to Home Screen" prompt on iOS and a native install prompt on Android; the site then launches full-screen with its own home-screen icon |
| **2.6.0** | Duplicate-topic detection on the Preview page: each pending article's title is compared (same-language, Jaccard on normalised tokens) against other pending items and against draft / scheduled / published articles from the last 14 days. Likely duplicates get a prominent orange warning with the matching title, source and similarity score, and the Discard button turns orange to nudge the action — keeps the same story from reaching the site twice when multiple feeds cover it |
| **2.5.3** | Human discards on the Preview page are now appended to the rejection log, under a dedicated "Discarded on Review" (`human-discarded`) category so they can be distinguished from AI-filter rejections. Keeps the "What We Skip" log complete and makes it possible to audit what a human chose to drop |
| **2.5.2** | Unify scheduling timezone across the codebase. `/api/suggest-schedule`, `/api/positronitron` and `/api/publish-scheduled` now share a single `admin/lib/schedule-time.ts` helper; `publish_date` is unambiguously wall-clock time in `SCHEDULE_TZ` (env-configurable, defaults to `Europe/Brussels`). Fixes the iPad bug where the suggested schedule returned times already in the past because the server produced UTC wall-strings while the client interpreted them as Brussels wall-strings |
| **2.5.1** | Reverting a published article from History back to the publishing queue now also deletes the corresponding markdown file from the live site's GitHub repo, so the article actually disappears from the public site instead of lingering while showing as a draft in admin. Factored the GitHub-file-delete logic into a shared helper reused by both revert and full-remove |
| **2.5.0** | OPML import / export on the Sources admin page: download all sources as a standard OPML 2.0 file (grouped by language) or bulk-import feeds from one. Duplicates are skipped — never overwritten — and flagged in the result panel with the matching existing source, so the user can verify whether an OPML entry reflects a rename or URL change |
| **2.4.0** | Positronitron automation modes: the single on/off toggle becomes a 4-way mode selector — `off`, `fetch only` (auto-fetch + filter), `fetch + summarise` (auto-drafts for human review), or `full` (end-to-end autonomous). In summarise mode, top articles land as drafts on the Scheduled page instead of auto-publishing. AUTO badge in nav adapts per mode |
| **2.3.0** | Cloud deployment: admin runs on Vercel + Turso, scheduled jobs via GitHub Actions every 30 min, configurable Positronitron run times from Settings, batch restore for fast data migration |
| **2.2.0** | Masonry card layout: replace CSS Grid equal-height rows with Masonry.js for a dynamic, variable-height card layout that eliminates white space. Featured cards still span double width. Reading order preserved left-to-right |
| **2.1.0** | Replace Python/Playwright Instagram card generator with @vercel/og (Satori) — portable to Vercel serverless, no external dependencies |
| **2.0.0** | Positron can now run fully autonomously. Per-section save buttons on Settings page, manual "Run now" trigger for Positronitron, and randomised publish scheduling for more natural-looking post times |
| **1.14.0** | Positronitron autonomous publishing mode: fully automated pipeline that fetches RSS, filters with max strictness, selects the top N most positive articles by score, summarises, schedules with social announcements, and marks the highest-scored as featured. Toggle on/off in Settings with configurable article count. Green "AUTO" badge in nav when active. launchd plist runs at 08:00 and 15:00 daily |
| **1.13.0** | Positivity scoring: LLM now assigns a 1-10 positivity score to every article during filtering. Scores are stored, displayed as colour-coded badges in the Scheduled and History admin pages, and will serve as the foundation for the upcoming Positronitron autonomous publishing mode |
| **1.12.2** | Fix publish timestamp: articles now use the actual publish time in front matter and filenames, not the scheduled time. Fixes incorrect timeline ordering when using "publish now" to override a scheduled slot. Skip SSR for Scheduled admin page to eliminate hydration mismatches |
| **1.12.1** | Fix scheduled-page hydration error (server/client mismatch from `Date.now()`). Dense grid layout (`grid-auto-flow: dense`) fills gaps around featured wide cards. Two-phase scheduled publish waits for GitHub Pages rebuild before posting to social (fixes 404 preview links) |
| **1.12.0** | Featured posts: "⭐ Featured (wide card)" toggle on Scheduled and History pages. Featured cards span two columns on the public site grid. Layout switched from flex-column round-robin to CSS grid for native responsive columns and `grid-column: span 2` support. Collapses to single column on mobile |
| **1.11.0** | Per-article "📣 Announce on social" checkbox on the Scheduled → Ready-to-publish list: when ticked, publishing the article (either manually or via the hourly `publish-scheduled` cron) also triggers a social media announcement through the existing Post-for-Me pipeline. Default is off, so social posting remains fully opt-in |
| **1.10.3** | Fix admin rejection-log totals: `/api/rejected` now returns full-table stats (total, top sources, by-category, uncategorised) alongside a 1,000-row browsable list, so the admin's numbers match the public site's "News We Skip" page instead of silently reflecting a capped sample |
| **1.10.2** | "Summarise all" bulk action on the Scheduled page — one click iterates every draft sequentially through the existing summarise endpoint, with per-row progress |
| **1.10.1** | Extend Mono and Mondrian theming to the About, Contact, What-We-Skip (negativity) and Archive pages so pills, cards, stats and pipeline boxes match the selected theme across EN/NL/FR |
| **1.10.0** | Optional site themes: "Mono" (modern black & white) and "Mondrian" (white bg with bold primary-color borders) selectable via a new "Colours" dropdown in the nav; language selector also converted to a labelled "Language" dropdown; each theme ships its own positron logo variant |
| **1.9.4** | Fix Twitter URL truncation: budget caption against actual URL length; count emoji as 2 chars (Twitter weighted); add URL liveness check with UI warning |
| **1.9.3** | Negativity page: cap browsable list to 1,000 most recent rejections; add EN/NL/FR section headings; client-side pagination (50/100/250); fix intro paragraph width |
| **1.9.2** | Secure `/api/publish-scheduled` with `ADMIN_SECRET` header check; launchd agent passes secret via `x-publish-secret` header |
| **1.9.1** | Fix Bluesky URL truncation: caption budget now accounts for full URL length (300-char Bluesky limit) instead of X's t.co 23-char shortening |
| **1.9.0** | Admin authentication (login page + session cookie middleware); database backup/restore; unified social publishing via Post for Me (Bluesky, X, Threads, Facebook, Instagram with auto-generated card); social account settings UI; scheduling timezone fix; Twitter double-post fix |
| **1.8.0** | Fast Track — one-click pipeline: fetch → filter (threshold 10, maximum strictness) → summarise → publish, with live streaming progress log |
| **1.7.7** | Fix instagram card generator python3 PATH; split share into "Copy link" and "Copy post" buttons on article pages |
| **1.7.6** | Instagram card generator — 📸 button on History page downloads a 1080×1080 PNG card per article |
| **1.7.5** | Make Ollama optional in start.sh/stop.sh; default is without Ollama, use --with-ollama to include it |
| **1.7.4** | Fix fetch progress bar exceeding 100% when sources error |
| **1.7.3** | Translate date-range dropdown, Clear button, and no-match message into NL and FR |
| **1.7.2** | Add social share buttons (X, Bluesky, Facebook, copy-for-Instagram) to article detail page |
| **1.7.1** | Show hero image on article detail page when image_url is present |
| **1.7.0** | Update Instagram handle on contact page to @positron_today |
| **1.6.0** | OpenAI ChatGPT as a third provider option; tunable positivity threshold slider (1–10) with live prompt preview and manual override; editable summarisation voice/style; auto-open browser on `start.sh` |
| **1.5.1** | Bug fixes: source deletion now cascades raw_articles (FK fix); favicon switched to white background; LLM reason crash fixed when model returns object instead of string |
| **1.5.0** | Instagram profile picture (Charged Luminism design philosophy); RSS links in footer and About page |
| **1.4.0** | Article editing UI (inline edit modal on Scheduled and History pages, Save & Republish); trilingual RSS feeds (EN/NL/FR); show only selected tags in Ready-to-publish section |
| **1.3.0** | og:image thumbnails captured during summarisation and displayed on article cards; round-robin column layout so newest articles appear at top row; mobile layout flash fix |
| **1.2.0** | Ollama activity log in separate Terminal window on start; site renamed Positron Today; "Why Positron?" section on About page (EN/NL/FR) |
| **1.1.0** | Source publication dates captured from RSS, shown on cards and in admin tables; date-range filter on homepage; dual dates in History and Rejections admin tables; `start.sh` / `stop.sh` scripts |
| **1.0.0** | Language switcher in nav bar (global, all pages); compact multi-select tag filter; 5 rejection categories; fetch counter and language restore fixes |
| **0.9.0** | Sortable rejections table; stop/reset backfill controls; Ollama model mismatch guard; settings empty-string fallback |
| **0.8.1** | History page redesigned as compact table with live-post link, source, tags, date, Republish / Re-summarise / Remove actions; Remove deletes the GitHub file |
| **0.8.0** | Configurable LLM providers — Anthropic or local Ollama per task; new Settings admin page with connection test and model browser |
| **0.7.0** | RSS feed support for all sources; streaming fetch progress; rejection log with auto-export; "What We Skip" public page (EN/NL/FR) |
| **0.6.0** | Date/month filter on homepage with localStorage persistence |
| **0.5.2** | Card layout; atom logo |
| **0.5.0** | Tag filtering; deterministic topic colours; many-to-many article_tags |
| **0.4.0** | Multilingual summaries (EN/NL/FR); language switcher |
| **0.3.0** | Claude Sonnet summarisation pipeline |
| **0.2.0** | Admin review workflow; manual URL submission |
| **0.1.0** | Initial release — basic RSS fetch and publish |
